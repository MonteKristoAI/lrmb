{
  "name": "gummygurl-blog",
  "version": "1.0.0",
  "private": true,
  "description": "GummyGurl blog - static HTML served via Cloudflare Workers proxy at gummygurl.com/blog/",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && npx serve dist -l 3002",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "serve": "^14.0.0"
  }
}
