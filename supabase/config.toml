project_id = "hfpvnsbiewudpqbtlvte"

[auth]
enable_signup = false
# This site_url is for local-supabase only. The remote project's Auth Site URL
# is set in the Supabase dashboard (Auth -> URL Configuration) to lrmb.vercel.app.
site_url = "http://localhost:3000"
additional_redirect_urls = [
  "http://localhost:3000/**",
  "http://localhost:5174/**",
  "https://lrmb.vercel.app/**",
  "https://*-montekristoais-projects.vercel.app/**",
]

[auth.mfa.totp]
enroll_enabled = true
verify_enabled = true

[auth.email]
enable_confirmations = true
max_frequency = "1m0s"

[functions.escalate-overdue-tasks]
verify_jwt = false

[functions.travelnet-webhook]
verify_jwt = false
